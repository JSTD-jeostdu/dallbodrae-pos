# 달보드레 — 특수학급 카페 POS 웹앱 PRD

> 작성일: 2026-09-20
> 작성자: 정우
> 버전: v0.1 (초안)
> 참고: `슈의 라면 가게 POS` (축제용, 2026-09) 의 구조를 물려받되 화면은 새로 설계

---

## 1. 개요

### 1.1 배경
중학교 특수학급이 학기 중 상시 운영하는 카페 **달보드레**의 주문·제조·호출·기록을 하나의 웹앱으로 관리한다.
장애를 가진 학생들이 **스스로 주문을 받고, 음료를 만들고, 손님을 부르는** 전 과정을 수행할 수 있도록 돕는 것이 목적이며,
특수학급 선생님은 코드를 열지 않고도 메뉴·재료·설정을 직접 바꿀 수 있어야 한다.

> '달보드레'는 '달콤하고 부드럽다'는 뜻의 순우리말이다. 앱의 말투와 색감도 그 결을 따른다.

### 1.2 목표
1. **학생의 직무 수행과 자립** — 글자를 읽기 어려워도 사진을 보고 주문을 받고 음료를 만들 수 있다
2. **운영 혼선 방지** — 주문 누락, "누구 거였지?"를 없앤다
3. **운영에 필요한 기록** — 어떤 메뉴가 얼마나 나가는지, 무엇을 장봐야 하는지가 저절로 쌓인다
4. **선생님이 직접 바꿀 수 있는 구조** — 메뉴·사진·재료·문구·글자 크기를 모두 앱 안에서 설정

### 1.3 비목표 (이번 버전에서 다루지 않는 것)
- **실제 결제·거스름돈 처리** — 값은 종이 쿠폰카드에 손님이 직접 도장을 찍는 방식으로 받는다
- 쿠폰카드 잔여 칸 관리, 손님(교직원) 명단·선호도 기록
- 음료 제조 단계별 안내(레시피 체크리스트) — 종이 레시피와 습관으로 해결
- 사전 주문(휴대폰·PC), 교무실 배달
- 로그인·계정 관리 (교실 내 기기만 사용, 관리 화면만 PIN으로 보호)

---

## 2. 사용자 및 운영 시나리오

### 2.1 사용자
| 역할 | 인원 | 사용 화면 |
|---|---|---|
| 캐셔 학생 | 1~2명 | 주문 화면 (태블릿 1대) |
| 바리스타 학생 | 1~2명 | 제조 화면 (태블릿 1대) |
| 손님 (교직원, 가끔 외부 손님) | 다수 | 호출 화면 (모니터, 보기 전용) |
| 특수학급 선생님 | 1명 | 관리 화면 (개인 기기) |

- 학생은 **3~5명이 역할을 돌아가며** 맡는다. 오늘의 담당은 선생님이 관리 화면에서 지정한다.
- 학생들의 읽기·숫자 수준 **편차가 크다.** 같은 화면을 쓰되 글자 크기를 즉시 바꿀 수 있어야 한다.

### 2.2 운영 흐름
```
손님이 카페로 옴
  → [주문 화면] 사진 타일 탭 → 담은 내용 확인 → "주문 완료" → 주문번호 발번
  → [제조 화면] 대기 카드에 자동 표시 → 음료 제작 → "완료" 터치
  → [호출 화면] 차임음 + 음성 안내 + 번호 초대형 표시
  → 손님이 수령 (쿠폰카드 도장은 손님이 직접)
```

### 2.3 운영 주기
- 학기 중 **상시 운영** (쉬는 시간·점심시간)
- **영업일은 기기 날짜 기준으로 자동 전환**한다. 날이 바뀌면 주문번호가 1번부터 다시 시작하고, 기록은 날짜별로 계속 쌓인다
- 선생님이 "영업 시작/종료"를 누르는 절차는 두지 않는다 (학생이 깜빡할 여지를 없앤다)
- 다만 번호가 너무 커졌거나 연습 주문이 섞였을 때를 위해, 관리 화면에서 **주문번호를 수동으로 초기화**할 수 있다 (3.6.1)

---

## 3. 기능 요구사항

### 3.1 주문 화면 (Order)

**3.1.1 메뉴 타일 — AAC 원칙 적용**
- 타일은 **정사각 사진 + 그 아래 큰 글자 이름**으로 구성한다 (그림과 글자를 항상 함께 제시)
- 사진이 등록되지 않은 메뉴는 **이모지**로 대체 표시한다
- 타일 최소 크기 160×180px, 타일 간 간격 14px 이상 (터치 오조작 방지)
- 탭할 때마다 수량 +1, 짧은 진동·효과음으로 "눌렸다"는 피드백을 준다

**3.1.2 담은 내용 (장바구니)**
- 담은 항목을 **사진 썸네일 카드**로 크게 표시한다 — 글자를 못 읽어도 무엇을 담았는지 보인다
- 각 카드에 `－ 수량 ＋` 버튼, 그리고 항목 삭제 버튼
- "전체 비우기"는 확인 1단계를 둔다

**3.1.3 품절 처리**
- 품절 메뉴는 **흐려지고 큰 ✕ 표시**가 겹쳐진다 (색만으로 구분하지 않는다)
- 탭해도 아무 일도 일어나지 않는다 — 누를 수 없는 버튼을 누르고 실패하는 경험을 만들지 않는다

**3.1.4 주문 확정**
- "주문 완료" 터치 시 **주문번호 발번** (당일 1번부터 순차, 트랜잭션으로 중복 방지)
- 레시피가 등록된 메뉴는 **재료 자동 차감**
- 제조 화면 대기 목록에 즉시 추가
- 완료 화면에 **주문번호를 초대형으로 표시**하고 음성으로도 안내한 뒤, 3초 후 자동으로 새 주문 화면으로 돌아간다

**3.1.5 주문 취소**
- 최근 주문 목록(최근 12건)에서 취소할 수 있다
- 취소 시 **재료 자동 복구**, 취소 이력은 별도 기록 (판매량 집계에서 제외)

**3.1.6 금액 표시 — 3단계 모드 (설정에서 선택)**

| 모드 | 동작 |
|---|---|
| **끔 (기본값)** | 금액을 어디에도 표시하지 않는다. 쿠폰제 운영에 맞는 기본 상태 |
| **가격 표시** | 타일과 장바구니에 가격, 하단에 합계를 표시한다 (숫자 읽기 연습) |
| **계산 훈련** | 합계 + 받은 돈 입력 키패드 + **거스름돈 자동 계산**까지 표시한다. 실제 결제가 아닌 **연습용**이며, 판매 기록에는 영향을 주지 않는다 |

> 데이터에는 **처음부터 메뉴별 `price` 필드를 둔다.** 지금은 꺼두더라도 나중에 켤 때 구조를 고칠 일이 없게 한다.

**3.1.7 글자 크기**
- 화면 오른쪽 위에 **보통 / 크게 / 아주 크게** 3단계 버튼을 상시 노출한다
- 학생마다 편차가 크므로 설정 깊숙이 넣지 않고 손 닿는 곳에 둔다
- 선택값은 기기별 localStorage에 저장된다

---

### 3.2 제조 화면 (Barista)

- 주문 완료된 건이 대기 목록에 **카드**로 자동 표시된다
- 카드 내용: **주문번호(대형) / 메뉴 사진 / 메뉴명 / 수량 / 경과 시간**
- 오래된 주문일수록 위에 놓이고, 일정 시간(기본 5분) 초과 시 색과 테두리로 강조한다
- **"완료" 버튼** 터치 → 호출 화면으로 전달 + 대기 목록에서 제거
- 실수 대비 **되돌리기** — 완료 직후 몇 초간(기본 6초) 취소할 수 있다
- 상단에 **"오늘의 바리스타: ○○"** 표시
- 메뉴 **품절 토글**을 이 화면에서도 조작할 수 있다 (재료가 떨어진 걸 먼저 아는 사람이 바리스타이므로)

---

### 3.3 호출 화면 (Display)

- 손님이 보는 전용 화면 (조작 불가, 전체화면 모드)
- 상단 큰 제목: **달보드레**
- 최근 호출된 **주문번호를 화면 중앙에 초대형**으로 표시
- 그 아래 직전 호출 번호 3~4개를 작게 나열
- **호출 연출**
  - 차임음 재생
  - 이어서 브라우저 내장 음성합성(Web Speech API)으로 안내
  - 기본 문구: `"{번호}번 손님, 음료 나왔습니다."` — 설정에서 수정 가능
  - 한국어 음성, 속도·볼륨 조절 가능
  - **숫자는 한자어 수사로 읽는다** — 한국어 TTS는 `3번`을 세는 단위로 보고 **[세 번]**으로
    발음한다. 주문번호는 횟수가 아니라 이름표이므로 **[삼 번]**이 맞다.
    화면에는 숫자를 그대로 크게 띄우고(손님이 번호표와 눈으로 맞춰야 한다),
    음성으로 넘길 때만 한글 수사로 바꾼다. 주문 완료 화면의 안내 음성도 같은 규칙을 따른다.
- 번호 텍스트 확대·깜빡임 애니메이션 병행
- 브라우저 자동재생 정책상 **시작할 때 화면을 한 번 터치**해야 소리가 난다 (안내 문구 표시)

---

### 3.4 재료 재고 (Ingredients)

**점진 도입이 원칙이다. 레시피를 비워두면 재고 기능은 조용히 비활성 상태가 된다.**
처음에는 재고 없이 쓰다가, 익숙해지면 메뉴 하나씩 레시피를 채워 넣을 수 있다.

| 기능 | 설명 |
|---|---|
| 재료 등록 | 이름 + 단위(ml / g / 개) + 현재 수량 + 발주 기준점 |
| 메뉴 레시피 | 메뉴마다 `재료 × 1잔당 사용량` 목록. 예: 아이스라떼 = 우유 150ml + 원두 18g + 아이스컵 1개 + 빨대 1개 |
| 자동 차감 | 주문 완료 시 판매 수량만큼 원자적으로 차감 |
| 자동 복구 | 주문 취소 시 복구 |
| 입고 | 장본 재료를 추가 입력 (이력 기록) |
| 폐기 | 실패한 음료·시음분을 **재고에서만 차감**, 판매량에는 미반영 (사유 메모 선택 입력) |
| **장볼 목록** | 발주 기준점 이하 재료를 자동으로 모아 보여준다. 복사·인쇄 가능 |
| 부족 경고 | 기준점 이하 재료를 쓰는 메뉴에 주문 화면에서 경고 배지 표시 |

> 폐기를 판매와 분리해야 "재고는 줄었는데 판매 기록이 안 맞는" 상황을 막을 수 있다.

**품절 토글** — 재료 재고와 무관하게, 선생님이 메뉴별로 "오늘 안 돼요"를 즉시 켤 수 있다. 관리 화면과 제조 화면 양쪽에서 조작한다.

---

### 3.5 기록 화면 (Report)

- 조회 단위: **오늘 / 이번 주 / 이번 달 / 기간 지정**
- **메뉴별 판매 잔 수** (막대그래프 + 표)
- 시간대별 분포 (쉬는 시간·점심시간대 파악용)
- 총 주문 건수, 운영일 수, 일평균 판매량
- 재료 대사표: 기초 재고 / 입고 / 판매 차감 / 폐기 / 현재 잔량
- 취소 내역 목록
- **CSV 내보내기** — 학생들과 함께 열어보거나 학기 말 정리 자료로 사용
- 금액 모드가 켜져 있으면 금액 집계도 함께 표시

---

### 3.6 관리 화면 (Settings) — PIN 보호

학생이 실수로 들어가지 않도록 **4자리 PIN**으로 잠근다 (기본값 제공, 변경 가능).

- **카페 정보** — 이름, 테마 색상
- **메뉴** — 이름 / 사진 / 이모지 / 가격 / 순서 변경 / 숨김 / 품절 / 레시피
- **재료** — 등록·수정·삭제, 입고, 폐기, 발주 기준점
- **오늘의 담당 학생** — 캐셔·바리스타 이름 지정 (주문·제조 화면 상단에 표시)
- **호출** — 음성 문구 템플릿, 속도·볼륨, 차임음 on/off, 호출 테스트 버튼
- **표시** — 금액 모드(끔 / 가격 표시 / 계산 훈련), 기본 글자 크기, 조리 지연 강조 기준(분)
- **기기 역할** — 주문 / 제조 / 호출 / 관리
- **주문번호 초기화** — 아래 3.6.1
- **데이터 초기화** — 2단계 확인. `dallbodrae_pos` 경로 안에서만 동작

#### 3.6.1 주문번호 초기화

주문번호는 날이 바뀌면 자동으로 1번부터 시작하지만, 그 사이에도 되돌려야 할 때가 있다.
연습 주문을 몇 건 넣어보고 실제 운영을 시작할 때, 번호가 너무 커져 학생이 읽기 어려울 때, 오전·오후 운영을 나누고 싶을 때가 그렇다.

- 관리 화면에 **"주문번호 다시 1번부터"** 버튼을 둔다
- **시작 번호를 직접 지정**할 수도 있다 (예: 1번, 또는 100번부터)
- **안전장치** — 아직 제조 중이거나 호출되지 않은 주문이 남아 있으면 그대로 진행하지 않는다

  > "아직 받지 않은 주문이 3건 있어요. 지금 번호를 되돌리면 같은 번호가 두 번 불릴 수 있습니다."

  대기 건수를 보여주고 확인을 한 번 더 받는다. 대기가 0건이면 확인 1단계로 끝난다
- **이미 만들어진 주문 기록은 지우지 않는다.** 번호 카운터만 되돌린다 — 판매량 집계는 그대로 유지된다
- 초기화와 함께 **호출 화면의 최근 호출 목록을 비울지**를 선택할 수 있다 (기본값: 비움)
- 초기화 시각·이전 번호·새 시작 번호를 기록에 남긴다. 같은 날 번호가 중복되었을 때 무슨 일이 있었는지 추적할 수 있어야 한다

---

## 4. 화면 구성 요약

| 화면 | 기기 | 주 사용자 | 조작 |
|---|---|---|---|
| 주문 (Order) | 태블릿 1대 | 캐셔 학생 | 터치 |
| 제조 (Barista) | 태블릿 1대 | 바리스타 학생 | 터치 |
| 호출 (Display) | 모니터 1대 | 손님 (보기 전용) | 없음 |
| 관리·기록 (Admin) | 선생님 기기 | 특수학급 선생님 | 터치/클릭 |

> 최초 접속 시 기기 역할을 선택하고, 선택값은 localStorage에 저장해 새로고침해도 유지한다.

---

## 5. 접근성 설계 원칙

이 앱의 사용자는 장애를 가진 학생이다. 아래 원칙은 기능보다 우선한다.

1. **그림과 글자를 항상 함께** — 사진 + 큰 글자. 둘 중 하나만 쓰지 않는다
2. **색에만 의존하지 않는다** — 품절은 흐림 + ✕ 아이콘, 지연은 색 + 테두리 + 아이콘
3. **누를 수 없는 버튼은 누르게 두지 않는다** — 실패 경험을 만들지 않는다
4. **모든 동작은 되돌릴 수 있다** — 완료 되돌리기, 주문 취소, 삭제 확인
5. **한 화면에 한 가지 일** — 주문 화면에서 재고를 고치지 않는다
6. **피드백은 즉시, 여러 감각으로** — 화면 변화 + 효과음 + 진동
7. **글자 크기는 학생이 직접** — 설정이 아니라 화면 위에서 바로
8. **선생님이 바꾸는 것과 학생이 쓰는 것을 분리** — 관리 화면은 PIN으로 잠근다

---

## 6. 기술 요구사항

### 6.1 아키텍처
- **단일 `index.html`** — 빌드 도구 없음
- **Vanilla JS**, 외부 라이브러리는 CDN only
- 상단 `CONFIG` 객체에 Firebase 키·상수·기본값 집약
- 코드 주석은 한국어

### 6.2 배포 및 데이터
- 배포: **GitHub Pages**
- 데이터베이스: **Firebase Firestore**
- 실시간 동기화: `onSnapshot` 구독으로 주문 → 제조 → 호출 화면 자동 반영
- **로컬 모드 폴백** — `CONFIG.firebase.apiKey`가 비어 있으면 localStorage 기반으로 동작한다. 같은 기기의 여러 탭끼리만 동기화되며, 연습·시연용으로 쓴다
- 데이터 경로를 `dallbodrae_pos/…` 아래로 분리하고 Firebase 인스턴스 이름도 `dallbodrae-pos`로 따로 둔다 — 같은 프로젝트의 다른 앱(라면 POS 등)과 절대 섞이지 않는다
- **Firebase 초기 세팅은 정우쌤이 맡아 완료한 상태로 전달한다.** 선생님은 주소만 열면 바로 쓴다

### 6.3 사진 저장 방식
- 업로드 시점에 앱이 **400×400 JPEG(품질 0.7)로 리사이즈**하여 Firestore 문서에 data URL로 저장한다 (장당 약 30~50KB)
- Firebase Storage를 쓰지 않는다 — 보안 규칙·CORS 추가 설정이 필요 없고, 오프라인 캐시에 사진까지 함께 남는다
- 메뉴 10~20개 기준 총 1MB 미만으로 무료 한도에 부담이 없다
- 문서 1MB 제한을 넘지 않도록 업로드 시 용량을 검사하고, 초과하면 품질을 낮춰 재시도한다

### 6.4 동시성 처리
- 문서 전체 덮어쓰기 금지 → **필드 단위 merge**
- 재료 차감은 `increment(-n)` 등 **원자적 연산**
- 주문번호 발번은 **트랜잭션** (일별 카운터 `counters/{영업일}`)
- Firestore 오프라인 지속성 활성화, 연결 복구 시 자동 동기화
- 트랜잭션은 인터넷 연결이 필요하다. 끊기면 주문 확정 시 안내를 띄우고, 화면은 캐시로 유지한다

### 6.5 UI/UX
- 터치 우선: 버튼 최소 크기 확보, 인접 버튼 간 여백 충분히
- 고대비 색상, 큰 폰트, 달보드레의 이름에 맞는 부드러운 색감
- 파괴적 동작(주문 취소·메뉴 삭제·초기화)은 확인 단계를 둔다
- 화면 하단 푸터: **Made by 정우쌤**

---

## 7. 데이터 모델 (초안)

`spaceId`는 운영 단위 구분자다 (`CONFIG.spaceId`, 예: `cafe-2026`).
학년도가 바뀌거나 처음부터 다시 시작하고 싶을 때 이 값만 바꾸면 이전 데이터와 섞이지 않는다.

```
dallbodrae_pos/{spaceId}/

  menus/{menuId}
    name, photo(dataURL|null), emoji, price, order,
    visible, soldOut,
    recipe: [ { ingredientId, qty } ]        ← 비어 있으면 재고 차감 안 함

  ingredients/{ingredientId}
    name, unit('ml'|'g'|'개'), current, reorderPoint, order

  orders/{orderId}
    orderNo, businessDate('YYYY-MM-DD'),
    items: [ { menuId, name, photo, price, qty } ],
    total,
    status('waiting'|'ready'|'cancelled'),   ← 주문번호가 초기화되면 같은 날 orderNo가
                                              중복될 수 있으므로, 목록 정렬과 식별은
                                              항상 orderId·createdAt 기준으로 한다
    cashier, barista,                        ← 주문 시점의 담당 학생 이름을 복사해 둔다
    createdAt, readyAt, cancelledAt          (이번 버전에서는 집계하지 않는다.
                                              나중에 학생별 수행 기록이 필요해지면
                                              데이터를 소급해 쓸 수 있게 남겨두는 것)

  stockLogs/{logId}
    ingredientId, type('restock'|'waste'|'sale'|'restore'|'adjust'),
    qty, memo, orderId(해당 시), createdAt

  counters/{businessDate}
    value                                    ← 트랜잭션으로 증가, 일별 1번부터

  counterResets/{resetId}                    ← 주문번호 초기화 이력 (3.6.1)
    businessDate, beforeValue, startFrom,
    waitingCount, clearedDisplay, createdAt

  settings/app
    cafeName, theme,
    priceMode('off'|'show'|'practice'),
    fontScale, tts{ text, rate, volume }, chime,
    todayStaff{ cashier, barista },
    lateMinutes, undoSeconds, pin
```

---

## 8. 개발 우선순위

| 단계 | 범위 |
|---|---|
| **P0 (필수)** | 역할 선택, 메뉴 등록(사진 업로드 포함), 주문 화면(타일·장바구니·주문번호 발번), 품절 토글, 제조 화면(대기·완료·되돌리기), 호출 화면(번호 + 차임음 + TTS), 오늘의 담당 학생 표시, 일별 판매 기록, 관리 PIN, **주문번호 초기화** |
| **P1 (중요)** | 재료 등록·레시피·자동 차감·복구, 장볼 목록, 주문 취소, 글자 크기 3단계, 기록 조회(주/월) + CSV 내보내기, 입고 기록 |
| **P2 (여유 시)** | 금액 모드(가격 표시 / 계산 훈련), 폐기 기록, 테마 색상, 시간대별 분포, 호출 문구 커스터마이즈, 데이터 초기화 마법사 |

> 금액 모드는 P2지만 **`price` 필드와 모드 분기점은 P0부터 데이터·구조에 포함**한다. 나중에 켤 때 개조가 필요 없게 한다.

---

## 9. 남은 확인 사항

1. **메뉴 구성** — 실제로 판매할 음료 품목과 개수 (타일 배치와 기본 이모지 세트 결정용)
2. **기기 사양** — 태블릿인지 크롬북인지, 화면 크기와 터치 지원 여부 (타일 크기 기준값 조정)
3. **호출 화면 모니터** — 확보 여부. 없으면 태블릿 3대째가 필요하다
4. **음성 확인** — 사용할 기기에 한국어 TTS 음성이 설치되어 있는지 (설정의 "호출 테스트"로 사전 확인)
5. **학생 이름 표기** — 실명인지 별명인지 (화면에 노출되므로 선생님 판단 필요)
6. **PIN 초기값 전달 방식** — 선생님께 어떻게 안내할지
7. **시범 운영** — 실제 운영 전 더미 주문으로 한 번 돌려보고 데이터 초기화를 권장

---

Made by 정우쌤
